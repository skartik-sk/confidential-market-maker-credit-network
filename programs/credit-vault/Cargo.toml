[package]
name = "confidential-credit-vault"
version = "0.1.0"
edition = "2021"
rust-version = "1.89.0"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[features]
default = []
bpf-entrypoint = []

[dependencies]
pinocchio = "0.11.1"
